---
name: optimize-performance
id: shipyard:optimize-performance
description: Optimize application performance — frontend, backend, queries, and databases — by measuring a baseline, finding the real bottleneck, and verifying every fix before keeping it. Use when there's a performance budget or SLA to hit, Core Web Vitals or load times need improving, N+1 query patterns need fixing, or profiling reveals a bottleneck.
---

This skill's full instructions live in `.agents/skills/optimize-performance/SKILL.md`. Read that file now and follow it exactly.
