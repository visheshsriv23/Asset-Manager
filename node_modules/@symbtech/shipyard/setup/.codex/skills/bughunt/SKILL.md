---
name: bughunt
id: shipyard:bughunt
description: Diagnose a bug or performance regression. Triages first — fixes an obvious, visible-cause issue directly, otherwise builds a tight, red-capable feedback loop before hypothesising, then reproduces, minimises, ranks hypotheses, instruments, and fixes with a regression test. Use when the user says "diagnose" or "debug this", reports something broken, throwing, failing, flaky, or unexpectedly slow, or asks why a test/build/deploy keeps failing.
---

This skill's full instructions live in `.agents/skills/bughunt/SKILL.md`. Read that file now and follow it exactly.
